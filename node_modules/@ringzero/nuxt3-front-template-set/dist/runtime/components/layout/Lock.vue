<template>
  <div
    v-if="lock.state.lock"
    class="Lock"
    :style="{
      top: getRect().top,
      left: getRect().left,
      width: getRect().width,
      height: getRect().height,
      borderRadius: getRect().radius,
      backgroundColor: lock.state.color ? lock.state.color : undefined,
    }"
  >
    <PartsProgress v-if="lock.state.icon" :size="60" :width="4" :color="'#fff'">
      <v-icon :icon="ICONS.LOCK" size="32"
    /></PartsProgress>
  </div>
</template>

<script setup lang="ts">
import { ICONS } from '../../composables/constants'
import { useLock, useActiveRect } from '../../composables/utility'
//------------------------------------------------------------------------------------------------------------
// 定数・変数（state）
//------------------------------------------------------------------------------------------------------------
const lock = useLock()

//------------------------------------------------------------------------------------------------------------
// メソッド
//------------------------------------------------------------------------------------------------------------
function getRect() {
  return useActiveRect(lock.state.level)
}
</script>

<style scoped>
.Lock {
  position: fixed;
  background-color: rgba(0, 0, 0, 0.4);
  display: flex;
  justify-content: center;
  align-items: center;
}
</style>
