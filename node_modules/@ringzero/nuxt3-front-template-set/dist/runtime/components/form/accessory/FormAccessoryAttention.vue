<template>
  <div
    class="FormAttention"
    :style="{
      color: edit ? (color ? useColor(color) : undefined) : '#ccc',
      borderColor: edit ? undefined : '#CCC',
      backgroundColor: edit ? undefined : '#EEE',
      margin: margin,
    }"
  >
    <p :class="{ list: list }" v-for="item in value">{{ item }}</p>
  </div>
</template>

<script setup lang="ts">
/**===================================================================================================================
   * 注意 (Labelに注意アイコン表示、色は基本WARNING色)
   ===================================================================================================================**/
import { type formProps } from '../../../components/common'
import { useColor } from '../../../composables/colors'
//------------------------------------------------------------------------------------------------------------
// 引数
//------------------------------------------------------------------------------------------------------------
interface Props extends formProps {
  edit?: boolean
  value?: string[]
  color?: string
  margin?: string
  list?: boolean
}
const props = withDefaults(defineProps<Props>(), {})
</script>

<style scoped>
@charset "UTF-8";
.FormAttention {
  position: relative;
  margin: 0 10px 16px 0;
  padding: 0;
  font-size: 12px;
  color: rgba(var(--r-color-warning), 1);
  border: 1px solid rgba(var(--r-color-warning), 1);
  background-color: rgba(var(--r-color-form), 1);
  border-radius: 8px;
  padding: 10px 10px 0px;
  width: 100%;
}
.FormAttention p {
  position: relative;
  margin-bottom: 8px;
  line-height: 16px;
}
.FormAttention .list {
  left: 16px;
}
.FormAttention .list::before {
  position: absolute;
  left: -16px;
  top: 0;
  content: "・";
}
</style>
