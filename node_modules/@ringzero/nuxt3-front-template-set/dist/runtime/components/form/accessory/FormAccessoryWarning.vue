<template>
  <div
    class="FormWarning"
    :style="{
      color: edit ? (color ? useColor(color) : undefined) : '#ccc',
      borderColor: edit ? undefined : '#CCC',
      margin: margin,
    }"
  >
    <p :class="{ list: list }" v-for="item in value">{{ item }}</p>
  </div>
</template>

<script setup lang="ts">
/**===================================================================================================================
   * 警告 (Labelに警告アイコン表示、色は基本error色)
   ===================================================================================================================**/
import { type formProps } from '../../../components/common'
import { useColor } from '../../../composables/colors'
//------------------------------------------------------------------------------------------------------------
// 引数
//------------------------------------------------------------------------------------------------------------
interface Props extends formProps {
  edit?: boolean
  value?: string[]
  color?: string
  margin?: string
  list?: boolean
}
const props = withDefaults(defineProps<Props>(), {})
</script>

<style scoped>
@charset "UTF-8";
.FormWarning {
  position: relative;
  margin: 0 10px 16px 0;
  padding: 0;
  font-size: 12px;
  font-weight: bold;
  color: rgba(var(--r-color-error), 1);
  width: 100%;
}
.FormWarning p {
  margin-bottom: 8px;
  line-height: 16px;
}
.FormWarning .list {
  margin-left: 14px;
}
.FormWarning .list::before {
  position: absolute;
  left: 0;
  content: "・";
}
</style>
