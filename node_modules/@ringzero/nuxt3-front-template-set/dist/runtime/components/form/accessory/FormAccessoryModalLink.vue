<template>
  <div
    class="FormModalLink"
    :style="{
      color: color ? useColor(color) : undefined,
      margin: margin,
    }"
  >
    <p class="link">{{ value ? value[0]?.text : 'リンク' }}</p>
  </div>
</template>

<script setup lang="ts">
/**===================================================================================================================
   * 注意 (Labelに注意アイコン表示、色は基本WARNING色)
   ===================================================================================================================**/
import { type formProps } from '../../../components/common'
import { useColor } from '../../../composables/colors'
//------------------------------------------------------------------------------------------------------------
// 引数
//------------------------------------------------------------------------------------------------------------
interface Props extends formProps {
  value?: any[]
  color?: string
  margin?: string
}
const props = withDefaults(defineProps<Props>(), {})
</script>

<style scoped>
.FormAttention {
  position: relative;
  margin: 0 10px 16px 0;
  padding: 0;
  font-size: 12px;
  color: rgba(var(--r-color-warning), 1);
  border: 1px solid rgba(var(--r-color-warning), 1);
  background-color: rgba(var(--r-color-background), 0.5);
  border-radius: 8px;
  padding: 10px 10px 4px;
}
.FormAttention p {
  position: relative;
  margin-bottom: 8px;
  line-height: 16px;
}
.FormAttention .link {
  color: rgba(var(--r-color-link));
}
</style>
