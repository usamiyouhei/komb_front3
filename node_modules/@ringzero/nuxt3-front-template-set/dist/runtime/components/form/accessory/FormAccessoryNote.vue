<template>
  <div
    class="FormNote"
    :style="{
      color: edit ? (color ? useColor(color) : undefined) : '#ccc',
      borderColor: edit ? undefined : '#CCC',
      margin: margin,
    }"
  >
    <p :class="{ list: list }" v-for="item in value">{{ item }}</p>
  </div>
</template>

<script setup lang="ts">
/**===================================================================================================================
   * 注釈 (基本info色、罫線で囲まれる)
   ===================================================================================================================**/
import { type formProps } from '../../../components/common'
import { useColor } from '../../../composables/colors'
//------------------------------------------------------------------------------------------------------------
// 引数
//------------------------------------------------------------------------------------------------------------
interface Props extends formProps {
  edit?: boolean
  value?: string[]
  color?: string
  margin?: string
  list?: boolean
}
const props = withDefaults(defineProps<Props>(), {})
</script>

<style scoped>
@charset "UTF-8";
.FormNote {
  position: relative;
  margin: 0 10px 16px 0;
  padding: 0;
  font-size: 12px;
  color: rgba(var(--r-color-info), 1);
  border: 1px solid rgba(var(--r-color-info), 1);
  border-radius: 8px;
  padding: 10px 10px 0px;
  width: 100%;
}
.FormNote p {
  position: relative;
  margin-bottom: 8px;
  line-height: 16px;
}
.FormNote .list {
  left: 16px;
}
.FormNote .list::before {
  position: absolute;
  left: -16px;
  top: 0;
  content: "・";
}
</style>
