<template>
  <div
    class="FormComment"
    :style="{
      color: edit ? (color ? useColor(color) : undefined) : '#ccc',
      borderColor: edit ? undefined : '#CCC',
      margin: margin,
    }"
  >
    <p :class="{ list: list }" :style="{ margin: listMargin }" v-for="item in value">{{ item }}</p>
  </div>
</template>

<script setup lang="ts">
/**===================================================================================================================
   * コメント(色はcolor色)
   ===================================================================================================================**/
import { type formProps } from '../../../components/common'
import { useColor } from '../../../composables/colors'
//------------------------------------------------------------------------------------------------------------
// 引数
//------------------------------------------------------------------------------------------------------------
interface Props extends formProps {
  edit?: boolean
  value?: string[]
  color?: string
  margin?: string
  list?: boolean
  listMargin?: string
}
const props = withDefaults(defineProps<Props>(), {})
</script>

<style scoped>
@charset "UTF-8";
.FormComment {
  position: relative;
  margin: 0 10px 16px 0;
  padding: 0;
  color: rgba(var(--r-color-color), 0.8);
  font-size: 12px;
  margin-bottom: 8px;
  width: 100%;
}
.FormComment p {
  margin-bottom: 8px;
  line-height: 16px;
}
.FormComment .list {
  margin-left: 14px;
}
.FormComment .list::before {
  position: absolute;
  left: 0;
  content: "・";
}
</style>
