<template>
  <div
    class="FormLine"
    :style="{
      borderBottomColor: color ? useColor(color) : undefined,
      margin: margin,
    }"
  />
</template>

<script setup lang="ts">
/**===================================================================================================================
   * フォーム内でラインを引くだけのパーツ
   ===================================================================================================================**/
import { type formProps } from '../../../components/common'
import { useColor } from '../../../composables/colors'
//------------------------------------------------------------------------------------------------------------
// 引数
//------------------------------------------------------------------------------------------------------------
interface Props extends formProps {
  color?: string
  margin?: string
}
const props = withDefaults(defineProps<Props>(), {})
</script>

<style scoped>
.FormLine {
  position: relative;
  margin: 0 10px 16px 0;
  padding: 0;
  height: 1px;
  border-bottom: 1px solid rgba(var(--r-color-line), 0.5);
}
</style>
