<template>
  <div
    class="FormHeadLine"
    :style="{
      color: color ? useColor(color) : undefined,
      margin: margin,
      border: border,
    }"
  >
    <h3 v-if="size === 1">{{ title }}</h3>
    <h4 v-if="size === 2">{{ title }}</h4>
    <h5 v-if="size === 3">{{ title }}</h5>
    <p v-if="subtitle">{{ subtitle }}</p>
  </div>
</template>

<script setup lang="ts">
/**===================================================================================================================
   * フォーム内でラインを引くだけのパーツ
   ===================================================================================================================**/
import { type formProps } from '../../../components/common'
import { useColor } from '../../../composables/colors'
//------------------------------------------------------------------------------------------------------------
// 引数
//------------------------------------------------------------------------------------------------------------
interface Props extends formProps {
  title: string
  size?: number
  color?: string
  border?: string
  margin?: string
  subtitle?: string
}
const props = withDefaults(defineProps<Props>(), {
  size: 1,
})
</script>

<style scoped>
.FormHeadLine {
  position: relative;
  width: 100%;
  min-width: 100%;
  margin: 20px 10px 24px 0;
  padding: 0;
  border-bottom: 1px solid rgba(var(--r-color-line), 1);
  display: flex;
  justify-content: space-between;
  align-items: flex-end;
}
.FormHeadLine p {
  color: #666;
  font-size: 14px;
}
</style>
