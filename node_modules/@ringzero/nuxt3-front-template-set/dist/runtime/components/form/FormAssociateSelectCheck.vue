<template>
  <FormBase
    class="FormSelectCheck"
    :base="base"
    :label="label"
    :rule="rule || data?.rule"
    :mode="mode"
    :edit="items[associate || 'default'] ? isEdit : false"
    :width="width"
    :col-width="colWidth"
    :label-width="labelWidth"
    :hint="hint"
    :hintchip="hintchip"
    :error="inputError"
    :result="result"
    :focus="focus"
    :line="false"
    :small="small"
    :medium="medium"
  >
    <PartsSelectCheck
      :value="items[associate || 'default'] ? input : ''"
      :inputType="SELECT_INPUT_TYPE.INPUT"
      :selectType="SELECT_LIST_TYPE.CHECK"
      :items="items[associate || 'default']"
      :disabled="items[associate || 'default'] ? !isEdit : true"
      :fit="fit"
      @update:value="onChange($event)"
    />
  </FormBase>
</template>

<script setup lang="ts">
/**
 * SelectCheckのフォーム用ラッパー
 *
 * associateで指定されたkeyのオブジェクトでアイテムを設定するメニュー
 *
 **/
import { FORM_MODE, type formProps, type formItem } from '../../components/common'
import { SELECT_INPUT_TYPE, SELECT_LIST_TYPE } from '../../components/common'
import { RULE, useValidation } from '../../composables/validation'
import { ref, watch, type Ref } from 'vue'

interface Props extends formProps {
  items: { [key: string]: any[] | undefined }
  associate?: string
  value?: string | number
  color?: string
  fit?: boolean
  rule?: any
}
const props = withDefaults(defineProps<Props>(), {})

const inputError: Ref<string | undefined> = ref(props.error)
const result: Ref<string | undefined> = ref()
const focus = ref(false)
const isEdit = ref(props.edit || props.base?.edit)

//------------------------------------------------------------------------------------------------------------
//watch
//------------------------------------------------------------------------------------------------------------
watch(
  () => props.value,
  (value) => {
    input.value = value
  }
)

watch(
  () => props.error,
  (value) => {
    inputError.value = value
  }
)

watch(
  () => props.data,
  (value) => {
    input.value = value?.value
    if (props.data) {
      if (value?.onCheck === 'check') {
        validation()
        props.data.onCheck = ''
      } else if (value?.onCheck === 'clear') {
        props.data.onCheck = ''
        props.data.error = ''
        inputError.value = ''
      }
      if (value?.error) {
        inputError.value = value?.error
      } else {
        inputError.value = ''
      }
    }
  },
  {
    deep: true,
  }
)

watch(
  () => props.edit,
  (value) => {
    if (value) {
      isEdit.value = true
    } else {
      isEdit.value = false
    }
  }
)

watch(
  () => props.base,
  (value) => {
    if (value?.edit) {
      isEdit.value = true
    } else {
      isEdit.value = false
    }
  }
)

//------------------------------------------------------------------------------------------------------------
// エミット
//------------------------------------------------------------------------------------------------------------
const emits = defineEmits<{
  (e: 'update:value', value: string): void
  (e: 'update:error', value: string): void
}>()

const input = ref(props.value)

function onChange(value: any) {
  input.value = value
  if (props.data) {
    props.data.value = value
  }
  validation()
  emits('update:value', value)
  emits('update:error', inputError.value || '')
}

//------------------------------------------------------------------------------------------------------------
// バリデーション
//------------------------------------------------------------------------------------------------------------
function validation() {
  if (props.data) props.data.isCheck = true
  const rule: RULE[] = props.rule || props.data?.rule || []

  const res = useValidation(String(input.value) || '', rule, result, props.data?.ruleParams)
  if (res === true) {
    inputError.value = ''
    if (props.data) props.data.error = ''
    return true
  } else {
    inputError.value = res
    if (props.data) props.data.error = res
    return false
  }
}
</script>
<style lang="scss" scoped></style>
