import { ref } from "vue";
import { defineStore } from "pinia";
export const useListMenuSuggest = defineStore(
  "listmenu-suggest",
  () => {
    const state = ref({});
    return {
      value: state.value
    };
  },
  {
    persist: process.client && {
      storage: sessionStorage
    }
  }
);
