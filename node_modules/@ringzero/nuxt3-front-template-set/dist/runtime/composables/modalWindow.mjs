import { ref } from "vue";
import { defineStore } from "pinia";
export const useModalSetting = defineStore(
  "modal-setting",
  () => {
    const state = ref({});
    return {
      value: state.value
    };
  },
  {
    persist: process.client && {
      storage: localStorage
    }
  }
);
