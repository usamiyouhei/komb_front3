import { useColor } from "./colors.mjs";
import { THEME_COLOR } from "./constants.mjs";
export const DIALOG_BUTTON = {
  CONFIRM: ["\u78BA\u8A8D"],
  YES_NO_JP: ["\u3044\u3044\u3048", "\u306F\u3044"],
  YES_NO: ["NO", "YES"],
  OK_CANCEL: ["CANCEL", "OK"],
  SUBMIT_CANCEL: ["CANCEL", "\u9001\u4FE1"]
};
export const DIALOG_COLOR = {
  ONE: [THEME_COLOR.primary],
  TWO: [THEME_COLOR.secondary, THEME_COLOR.primary],
  THREE: [THEME_COLOR.secondary, THEME_COLOR.warning, THEME_COLOR.primary]
};
export function useSetColors(colors) {
  const result = [];
  colors.forEach((color) => {
    result.push(useColor(color));
  });
  return result;
}
export function useDialogTypeNum(button, color) {
  const result = { buttons: button, colors: color };
  return result;
}
